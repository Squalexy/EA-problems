
# alinea a
void collinear(){
    for (int i = 1; i < N; i++){´
        for (int j = i + 1; j < N; j++){
            for(int k = i +1; k < N; k++){
                a = p[i];
                b = p[j];
                c = p[k];

                if (ay - by / ax - bx == by - cy / bx - cx)) return true;  
            }
        }    
    }
    return false;
}

# alinea b
