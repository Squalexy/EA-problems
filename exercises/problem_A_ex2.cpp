#include <bits/stdc++.h>

using namespace std;

# verificar se existem violações com um k dado
void check(G){
    c = 0;
    for each {i,j} in E do
        if color(i) == color(j) then
            c = c + 1
    return c
}

# descobrir k mais baixo possível para recolorir o grafo sem haver violações
# pesquisa binária
void A(G, k){

}

int main(){

    vector<int> v;
    vector<vector<int>> e;

}